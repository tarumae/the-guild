<%# <% if policy(Post).create? %>
  <%# <%= link_to "New Post", new_guild_post_path %>
<%# <% end %> 

<div class="container">
  <%= simple_form_for [@guild, @post] do |f| %>
    <h1>Description: </h1>
    <%= f.input :description, label: false %>
    <%= f.button :submit, class: "btn btn-flat"%>
  <% end %>
</div>